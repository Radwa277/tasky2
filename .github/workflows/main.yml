on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout"v3
      
      - name: "login to Docker hub"
        run: Docker name --username ${{ secrets.DOCKER_USERNAME}} --password GITHUBTOKEN
